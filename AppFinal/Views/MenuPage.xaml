<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppFinal.Views.MenuPage"
             Title="Menú principal"
             BackgroundColor="#FFF6F0">

    <VerticalStackLayout Padding="20" Spacing="25" VerticalOptions="Center">
        <!-- LOGO -->
        <Image Source="thespeaknowcub.png"
               HeightRequest="120"
               HorizontalOptions="Center" />

        <!--Titulo-->
        <Label Text="The Speak Now Club"
               FontSize="32"
               HorizontalOptions="Center"
               TextColor="#1F1F1F"/>

        <!--Botones del menú-->
        <Button Text="📋 Registrar Alumno"
                FontSize="18"
                BackgroundColor="#5A4B81"
                TextColor="White"
                Clicked="OnRegistrarAlumnoClicked"/>

        <Button Text="📅 Agendar Conversación"
                FontSize="18"
                BackgroundColor="#5A4B81"
                TextColor="White"
                Clicked="OnAgendarConversacionClicked"/>
    </VerticalStackLayout>
</ContentPage>