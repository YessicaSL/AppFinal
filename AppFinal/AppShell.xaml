<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AppFinal.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AppFinal"
    xmlns:vi="clr-namespace:AppFinal.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="The Speak Now CLub">

    <!-- Pantalla inicial -->

    <ShellContent
        Title="Inicio"
        ContentTemplate="{DataTemplate vi:InicioPage}"
        Route="InicioPage" />

    <!-- Rutas a otras páginas -->
<!--
    <ShellContent Title="Menú"
                  ContentTemplate="{DataTemplate vi:MenuPage}"
                  Route="MenuPage" />
    -->
</Shell>
